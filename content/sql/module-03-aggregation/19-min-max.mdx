---
title: "MIN и MAX"
objectives:
  - "Находить минимум и максимум с MIN/MAX"
---

## Theory

`MIN` и `MAX` возвращают наименьшее и наибольшее значение столбца соответственно — работают и с числами, и с датами, и с текстом (по алфавиту).

```sql
SELECT MIN(столбец), MAX(столбец) FROM таблица;
```

## Example

```sql
SELECT MIN(price), MAX(price) FROM products;
```

Вернёт самую низкую и самую высокую цену товара одним запросом.

## Common mistakes

- Писать два отдельных запроса вместо одного с MIN и MAX в одном SELECT.

