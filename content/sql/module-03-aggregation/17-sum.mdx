---
title: "SUM"
objectives:
  - "Считать сумму с помощью SUM"
---

## Theory

`SUM` складывает все значения числового столбца в результирующем наборе строк.

```sql
SELECT SUM(столбец) FROM таблица;
```

## Example

```sql
SELECT SUM(amount) FROM payments;
```

Посчитает суммарную сумму всех платежей.

## Common mistakes

- Пытаться применить SUM к текстовому столбцу — СУБД вернёт ошибку типов.

