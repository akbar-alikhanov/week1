---
title: "AVG"
objectives:
  - "Считать среднее с помощью AVG"
---

## Theory

`AVG` возвращает среднее арифметическое значений числового столбца, игнорируя NULL.

```sql
SELECT AVG(столбец) FROM таблица;
```

## Example

```sql
SELECT AVG(price) FROM products;
```

Посчитает среднюю цену товара.

## Common mistakes

- Ожидать целое число от AVG для integer-столбца — результат может быть дробным.

